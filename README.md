# SETU
A forecasting tool that predicts which vehicles/equipment will need overhaul or specific spares next, using usage and failure-history data, so provisioning happens before a backlog forms instead of after.
